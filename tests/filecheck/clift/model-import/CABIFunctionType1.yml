#
# This file is distributed under the MIT License. See LICENSE.md for details.
#
# RUN: %root/bin/revng2 pipeline run-pipe import-types %s /dev/stdout | %root/bin/revng clift-opt | FileCheck %s

Architecture: x86_64
TypeDefinitions:
  # CHECK: !_type_definition_1001_CABIFunctionDefinition = !clift.func<
  # CHECK:   "/type-definition/1001-CABIFunctionDefinition"
  # CHECK:   : !void()
  # CHECK:   [
  # CHECK:     #clift.c_attribute<
  # CHECK:       "_ABI" : "/macro/_ABI"
  # CHECK:       [
  # CHECK:         #clift.identifier<"SystemV_x86_64">
  # CHECK:       ]
  # CHECK:     >
  # CHECK:   ]
  # CHECK: >
  - ID: 1001
    Kind: CABIFunctionDefinition
    Name: "my_function1"
    ABI: SystemV_x86_64
    Arguments: []
